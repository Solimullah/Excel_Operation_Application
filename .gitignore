# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment
.env
.env.local
.env.*.local

# Working documentation is tracked, EXCEPT the personal notes:
# project-memory.md records working preferences and verbatim quotes and
# stays on this machine only.
context/project-memory.md

# Test coverage output
coverage

# TEMPORARY: pushing .github/workflows/ requires a token with the `workflow`
# scope. The file exists locally and is ready to go - delete these two lines
# and `git add .github/` once the token is updated.
.github/
